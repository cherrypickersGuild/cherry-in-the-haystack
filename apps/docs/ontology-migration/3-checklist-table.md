# GraphDB 마이그레이션·동기화 — 검수 체크리스트

> 범례: `-` 미착수 · `W` 진행 중 · `T` 테스트 통과 · `✅` 검수 완료.
> 🔴 표시 항목은 **공유 프로덕션 DB 쓰기**. 승인 없이 실행 금지.

## Phase 0 — 준비·확인
| 항목 | 상태 | 메모 |
|---|---|---|
| 0-1 GraphDB 로컬 기동 + 302개 로드 확인 | - | AI · 트리플 1,220 |
| 0-2 작성자 export 수신 여부 확인(G1) | - | 사용자 · 미수신이면 302개로 진행 |
| 0-3 갈림길 G1~G4 결정 | - | 사용자 |
| 0-4 [게이트] 지침·구현서 승인 | - | 사용자 |

## Phase 0-B — GraphDB 보완 (로컬 · 이관 **전에** 수행) ⭐
| 항목 | 상태 | 메모 |
|---|---|---|
| B-1 `decisions.json` 승인 항목만 추림 | - | 미검토 항목 반영 금지 |
| B-2 중복 라벨·설명 정리(`Alignment`·`InferenceOptimization`) | - | canonical 1개 + alias |
| B-3 노드 신설(`Reranking`·`Contextual Retrieval`·`GraphRAG`) | - | 승인 시 |
| B-4 관계 유형 서술어 정의(`prerequisite`/`extends`/`related`) | - | R7 해소 |
| B-5 누락 관계 트리플 추가 | - | |
| B-6 보완 후 TTL export → **리포에 커밋**(R11) | - | 재현·감사 가능성 |
| B-7 [게이트] 보완 결과 보고 | - | 사용자 |

## Phase 1 — 추출 (읽기 전용)
| 항목 | 상태 | 메모 |
|---|---|---|
| 1-1 `export-graphdb.cjs` → 스냅샷 파일 생성 | - | AI · `infer=false` |
| 1-2 스냅샷 건수 = 개념 302 · 관계 301 · 설명 308 | - | 불일치 시 원인 규명 후 진행 |
| 1-3 스냅샷을 파일로 고정(이후 재질의 금지) | - | 재현·감사 가능성 |

## Phase 2 — 사전 점검 (dry-run · 쓰기 없음) ⭐
| 항목 | 상태 | 메모 |
|---|---|---|
| 2-1 라벨 2개 클래스 목록 → canonical 지정 | - | `Alignment`·`InferenceOptimization` |
| 2-2 **alias 전역 유니크 충돌 0건**(R2) | - | 1건이라도 있으면 **중단·보고** |
| 2-3 `canonical_name` 대소문자 무시 중복 0건(R3) | - | |
| 2-4 관계 양끝 개념 모두 존재 | - | 누락 시 보고 |
| 2-5 사이클 0건(R9) | - | |
| 2-6 **삽입 예정 건수 표 제시** | - | AI → 사용자 |
| 2-7 [게이트] 🔴 **쓰기 승인** | - | 사용자 · 승인 없이 Phase 3 금지 |

## Phase 3 — 삽입 🔴
| 항목 | 상태 | 메모 |
|---|---|---|
| 3-1 마이그레이션 SQL(테이블+enum) 실행 | - | 사전 제시본과 동일한지 대조 |
| 3-2 단일 트랜잭션 · INSERT 만 · `ON CONFLICT DO NOTHING` | - | |
| 3-3 `concept` 삽입 (meta_json 에 ontologyNode 포함) | - | D4 |
| 3-4 `concept_alias` 삽입 | - | 2-2 통과분만 |
| 3-5 `concept_relation` 삽입 (방향 규약 준수) | - | "from 은 to 의 type 이다" |
| 3-7 **`id` 명시 생성 확인** — 기존 테이블 id 기본값 없음 | - | 누락 시 NOT NULL 위반 |
| 3-8 `topic_id` 전부 NULL 확인 | - | §2-A · Learning 소관 |
| 3-9 `RELATED` 중복 방향 0건 | - | 대칭 관계는 한 방향만 |
| 3-6 롤백 스크립트 준비·보관 | - | 실행 전에 준비 |

## Phase 4 — 검증
| 항목 | 상태 | 메모 |
|---|---|---|
| 4-1 건수 대조: 스냅샷 ↔ DB | - | 302 / 301 |
| 4-2 **라운드트립 완전 일치**(라벨·설명·관계 방향) | - | 불일치 0 |
| 4-3 샘플 육안: `RAG` 하위 = GraphDB 질의 결과 | - | 4개 |
| 4-4 사이클 재검사 | - | |
| 4-7 **`writer_agent /input` 응답 불변 확인** | - | 이관 전/후 동일해야(link 0이라 빈 응답 유지) |
| 4-5 기존 데이터 무영향 확인(`key_ideas` 3,067 · `paragraph_chunk` 3,054 불변) | - | 건수 전/후 대조 |
| 4-6 [게이트] 검증 결과 보고 | - | 사용자 |

## Phase 5 — 공급자 전환 구조
| 항목 | 상태 | 메모 |
|---|---|---|
| 5-1 `ConceptRelationProvider` 인터페이스 + Postgres 구현 | - | |
| 5-2 `CONCEPT_RELATION_SOURCE` env (기본 `postgres`) | - | 백엔드 env |
| 5-3 GraphDB 구현(기존 `graph-concept.service` 재사용) | - | |
| 5-4 세 구현의 응답 DTO 동일성 테스트 | - | |
| 5-5 `relation-source-diff` 스크립트 | - | 전환 안전 장치 |
| 5-6 `tsc --noEmit` 신규 에러 0 | - | 기존 kaas 8건 제외 |

## Phase 6 — (후속) 동기화
| 항목 | 상태 | 메모 |
|---|---|---|
| 6-1 GraphDB→Postgres 단방향 동기화 잡 | - | 수동 실행 우선 |
| 6-2 사람이 넣은 관계(`origin<>'graphdb-import'`) 보존 확인 | - | |
| 6-3 삭제 반영(`revoked_at`) 규칙 검증 | - | R8 |

## 성과 목표 (완료 기준)
- **Phase 4 시점**: Postgres 만으로 RAG 의 상위·하위 관계를 정확히 조회할 수 있고, 라운드트립 불일치 0.
- **Phase 5 시점**: `CONCEPT_RELATION_SOURCE` 한 줄로 소스가 바뀌며 **화면·API 코드 수정이 없다.**
- 전 구간: 기존 데이터 변경 0 · 물리 삭제 0 · 승인 없는 쓰기 0.
