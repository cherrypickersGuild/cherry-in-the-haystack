# 작업 로그 (2026-04-09)

---

## [커밋 이력 복원] 2026-04-01 이후 작업 요약

> git log 기반으로 역산 기록. 이후 작업부터는 실시간 기록.

### `56a00d5` — fix: mock_data set 400
- **범위**: `staged_mock/` SQL 파일 다수, `pipeline/agent-json-parser.service.ts`, `stats/landing-stat.service.ts`
- **내용**: mock 데이터 400건 기준으로 확장. stage-1/stage-4 extra-200 추가. rollback 스크립트 추가.

### `17d3b12` — fix: cron schedule
- **범위**: `schedule/ingestion-schedule.service.ts`
- **내용**: cron 식 수정.

### `c1a78fb` — fix: deploy
- **범위**: `.github/workflows/deploy.yml`
- **내용**: 배포 파이프라인 수정.

### `87b5c34` — fix: frameworks
- **범위**: `stats/frameworks-rank.service.ts`, `nd-frameworks-page.tsx`, `nd-model-updates-page.tsx`, `nd-case-studies-page.tsx`, `patch-notes-page.tsx`
- **내용**: Frameworks 관련 백엔드/프론트엔드 버그 수정.

### `a001b3e` / `85df4ef` — fix+feature: landing page
- **범위**: `stats/landing-stat.service.ts`, `web/app/page.tsx`
- **내용**: 랜딩 통계(treemap + momentum) API 구현 및 프론트엔드 홈 페이지 연결.

### `18cfd66` — fix: scheduler env guard
- **범위**: `schedule/ingestion-schedule.service.ts`
- **내용**: `SCHEDULER_ENABLED=true` 환경변수 없으면 자동 실행 비활성화.

### `14d6ea5` — feature: case-studies page
- **범위**: `patch_notes/case-studies.service.ts`, `nd-case-studies-page.tsx`
- **내용**: Case Studies 백엔드 서비스 + 프론트엔드 페이지 구현.

### `52eb8e3` — feature: frameworks_weekly_rank
- **범위**: `stats/frameworks-rank.service.ts`
- **내용**: FRAMEWORKS 주간 순위 집계 서비스 신규 구현.

### `168c3e2` — feature: schedule controller
- **범위**: `schedule/schedule.controller.ts`
- **내용**: `run-pipeline`, `run-daily-stats` 수동 트리거 엔드포인트 추가.

### `9dbba54` — feature: FRAMEWORKS mock data
- **범위**: `staged_mock/stage-1-frameworks-extra.sql`, `stage-4-*`
- **내용**: FRAMEWORKS 카테고리 전용 seed SQL 50건 추가.

### `e078b31` — feature: scheduler + model-updates rank
- **범위**: `schedule/`, `stats/model-updates-rank.service.ts`
- **내용**: 스케줄러 모듈 신설. MODEL_UPDATES 순위 집계 API 구현. 카테고리 기반 아티클 색상 추가.

### `cf3f11c` — feature: patch note
- **범위**: `patch_notes/` 모듈 전체
- **내용**: 패치노트 모듈 신설 (service, case-studies.service, controller).

### `894c466` — feature: agent_json_raw
- **범위**: `pipeline/agent-json-parser.service.ts`, `staged_mock/stage-4-agent-json-raw.sql`
- **내용**: agent_json_raw 파싱 서비스 구현 및 stage-4 seed 데이터 추가.

---

## [실시간] 2026-04-09

### buzz-treemap Binary Recursive 트리맵으로 교체 (Mondrian 스타일)

- **작업 범위**: `apps/web/components/cherry/buzz-treemap.tsx`
- **작업 내용**:
  - Squarified 알고리즘 제거 (와이드 컨테이너에서 얇은 가로 막대 문제)
  - Binary Recursive 알고리즘(`binaryRec`) 구현 — 아이템을 합이 균등한 두 그룹으로 분할 후 긴 변 방향으로 재귀 분할, 몬드리안 블록 구조 생성
  - `useRef` + `ResizeObserver`로 컨테이너 실측, `position: absolute` 절대좌표 배치
  - 색상/디자인 변경 없음
- **미완료/후속 과제**: 없음

---

### buzz-treemap 스쿼리파이드 트리맵으로 교체 (롤백됨)

- **작업 범위**: `apps/web/components/cherry/buzz-treemap.tsx`
- **작업 내용**:
  - 기존 CSS flex 행 분할 방식(`buildRows`) 제거
  - Squarified Treemap 알고리즘(`squarify`) 구현 — 면적이 percent에 비례하고 가로세로 비율이 정방형에 가깝게 유지됨
  - `useRef` + `ResizeObserver`로 컨테이너 실제 픽셀 크기 측정 후 절대좌표(`position: absolute`) 배치
  - 폰트 스케일 기준을 percent → 타일 면적(`√(w×h)`) 기반으로 변경
  - 타일이 너무 작을 경우 label/badge 자동 숨김 처리
  - 색상/디자인(bgColor, color, 그라디언트, 그림자, 뱃지 스타일)은 변경 없음
- **미완료/후속 과제**: 없음

---

### [오늘] agent-read 문서 갱신

- **브랜치**: main
- **작업 범위**:
  - `apps/docs/agent_read/agent-policy.md` (신규 생성)
  - `apps/docs/agent_read/api-backend-status-2026-04-09.md` (신규 생성)
  - `apps/docs/agent_read/work-log-2026-04-09.md` (신규 생성, 이 파일)
- **작업 내용**:
  - 에이전트 작업 정책 파일 신규 작성. 작업 로그 필수 규칙, 범위 원칙, 보안 원칙 정의.
  - 백엔드/프론트엔드 현황 문서를 2026-04-01 기준에서 2026-04-09 기준으로 갱신. 신규 모듈(pipeline, pipeline_user, patch_notes, stats, schedule) 및 프론트엔드 컴포넌트 전체 반영.
  - 엔드포인트 전체 목록, 데이터 파이프라인 흐름, staged_mock 현황, 이슈/미완료 항목 정리.
- **미완료/후속 과제**:
  - `api-backend-status-2026-04-01.md`는 구버전으로 보존 (삭제 안 함)
  - 이후 코드 변경 시 당일 work-log 파일에 항목 추가 후 status 문서 갱신
