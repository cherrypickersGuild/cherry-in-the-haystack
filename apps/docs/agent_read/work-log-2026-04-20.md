# 작업 로그 (2026-04-20)

> 이전 로그: `work-log-2026-04-12.md`
> 범위: 2026-04-13 ~ 2026-04-20 (8일치, kaas 모듈 전체 + 여러 feature).
> 방식: `git log` 기반 역산 요약. 세부는 `handoff-2026-04-17.md` 및 신규 `api-backend-status-2026-04-20.md` 참조.

---

## [커밋 이력 요약] 2026-04-13 → 2026-04-20

### `5817e08` — feature: KaaS (해커톤 주 제품 시작점)
- **범위**: `apps/api/src/modules/kaas/` 모듈 전체 신규, `apps/docs/staged_mock/kaas-*.sql`, 프론트 `components/cherry/kaas-*-page.tsx`, `apps/contracts/` Hardhat 프로젝트, `apps/agent-package/` MCP 에이전트 독립 패키지
- **내용**: Knowledge-as-a-Service 플랫폼 최초 구현. Chain Adapter 인터페이스 (status/near/mock), Concept/Evidence CRUD, Credit/Reward 서비스, MCP 통신 게이트웨이, 관리자/카탈로그/대시보드 프론트 3종

### `4e52570`, `6bc4783` — feature: NEAR AI / 보안처리
- NEAR AI TEE 파이프라인 기반 코드 + 초기 보안 처리.

### `bb6e940` — feature: NEAR protocol
- NEAR chain adapter 구현 (`chain-adapter/near-adapter.ts`).

### `5d24b4d` — feature: cherry protocol
- CherryCredit 컨트랙트 / provenance 기록 프로토콜 정립.

### `5404871` — minor changes in substack discoverer
- 사용 중단된 substack discoverer 정리 (not using it anymore).

### `e7e24e9` — feature: ready to hoodi
- Hoodi 배포 준비 (이후 RLN 플랫폼 버그로 Sepolia 유지).

### `23ba02c`~`6786f9d` — feature: design Model page (1~4)
- Model 업데이트 페이지 디자인 개선 4연속.

### `58f7124`, `6041bdd` — feature: NEAR Agent Market
- NEAR Agent Market 등록 + Knowledge Market 헤더 "LISTED ON / NEAR Agent Market" 2줄 카드 배지 (외부 링크 market.near.ai).
- 파일: `kaas-catalog-page.tsx`.

### `1012800` — feature: Sale Badge
- 카테고리별 top quality 1개씩 SALE 배지 (총 3개). 우상단 코너 탭 스타일, Compare 뱃지는 우하단.
- 백엔드: `KaasKnowledgeService.getOnSaleIds()` / `isOnSale()`. Purchase 20→16cr, Follow 25→20cr. Karma 할인과 곱연산 스택.
- 파일: `kaas-catalog-page.tsx`, `kaas-knowledge.service.ts`, `kaas-query.controller.ts`, `kaas-mcp.controller.ts`.

### `8a6eb40` — feature: cherry talk
- Cherry Console 대화 시스템 베이스.

### `fe55944` — feature: mcp error handling and logging
- MCP 채널 에러/로깅 강화.

### `0cc3f97`, `fadbfb8`, `b74eea3` — fix: MCP address / MCP agent (2)
- 초기 MCP 연결 버그 수정 시리즈.

### `1b4d290`, `3ac9ee3`, `e6812de` — feature: MCP agent - local (1~3)
- 서버 내부 데몬(`kaas-agent-daemon.service.ts`)에서 **독립 npm 패키지**로 전환. `apps/agent-package/` 구조, DB 직접 접속 없이 서버 HTTP API + WebSocket만 사용.
- 6개 MCP 도구 정의: `search_catalog`, `get_concept`, `purchase_concept`, `follow_concept`, `compare_knowledge`, `generate_self_report`.
- esbuild 번들 → `apps/web/public/cherry-agent.js` (1.7MB).

### `1e52fb5`, `a89aedc` — feature: MCP agent - remove previous agent registration
- 재등록 시 이전 api_key 자동 정리.

### `eb17bc9`, `bdbb0eb` — feature: welcome credit auto-credit on (1~2)
- 에이전트 등록 시 **200cr 자동 지급** (`depositDbOnly`, 온체인 미사용). 등록 후 `loadAgents()` 호출로 잔고 즉시 반영. Ledger에 "🎁 welcome" 표시.

### `54cfd36` — feature: auto credit reload
- 크레딧 잔고 주기적 갱신.

### `2538bf3`, `2acb276` — feature: report to cherry (1~2)
- 대시보드 "Connection Test" 섹션, MCP 에이전트 → `generate_self_report` → WS → 프론트 수신.

### `6cf6c52`, `c4cff9b`, `639291f` — feature: cherry for windows (+ mcp 업데이트)
- Windows 래퍼 `cherry-kaas.bat` 추가. PowerShell 설치 커맨드 정립.
- 대시보드에 Windows/Mac 토글 스위치 (OS별 명령어).

### `8240c0d` — fix: disable withdraw
- Withdraw 프론트 버튼을 "Not yet" 임시 메시지로 처리 (API는 구현 완료).

### `6d77835`, `759d564` — docs / feature: personalize agent registration
- 에이전트 개인화 기획서 (`apps/docs/KaaS/agent-personalization.md`) + 구현.
- `kaas-agent.controller.ts`: 컨트롤러 레벨 `@UseGuards(AuthGuard('jwt'))` 제거 → 엔드포인트별 개별 guard. `GET /me`만 api_key, 나머지 JWT + `verifyOwnership()`.
- `kaas-query.controller.ts`, `kaas-credit.controller.ts`: `SYSTEM_USER_ID` fallback 제거 → `api_key` 필수.
- `api.ts`: `authHeaders()` 헬퍼 추가.

### `617db6c` — feature: polling for WebSocket fallback
- Socket.IO transports `["websocket"]` → `["polling", "websocket"]`. Docploy 폴링 fallback 대응.

### `46d7dca` — fix: websocket
- URL 정규식 버그 수정: `.replace(/\/api.*$/, "")` 제거 (api.solteti.site 의 `api` 가 도메인째 잘리던 문제).

### `84c4278` — fix: MCP api - fix report
- self-report 중복 브로드캐스트 방지. `handleAgentReport`에서 `triggered_by === 'request'`이면 broadcast skip.

### `0b24661` — fix: dashboard to user
- 대시보드 유저 귀속 로직 보정.

### `e9cbcdb`, `8b43c35` — fix / feature: NEAR AI TEE apply + privacy
- NEAR AI TEE 적용 + 프라이버시 업데이트.

### `b678ab9` — feature: personalize rewards
- 리워드 개인화 (유저/에이전트별 분리).

### `d1f6cd4` — feature: concept reader market link
- Concept Reader 페이지에서 Knowledge Market으로 이동하는 링크.

### `227c1aa` — fix: popup bug
- 팝업 UI 버그 수정.

### `8d57a13`, `f5ba2b7`, `7bc416c` — feature: tree map (1~3)
- Buzz Distribution 트리맵 전면 개편. `overflow-hidden` 제거 (% 배지 잘림 방지), 배지 `absolute` 포지션, 배경 `rgba(0.78)` → `#ffffff` 완전 불투명, 호버 그라디언트 `borderRadius: 14` 자체 적용.
- 파일: `apps/web/components/cherry/buzz-treemap.tsx`.

### `b0fd191` — feature: curation - update
- 큐레이션 패널 업데이트.

### `4a28826` — fix: chrome button styling and responsiveness
- Chrome 버튼 스타일 + 반응형 보정.

### `7320314`, `4d62641` — fix: minor / removed some file mixup
- 소규모 버그 + 파일 정리.

### `db6eb86` — Update README.md

### `37b3d00` — WIP: NEAR wallet integration (pre-deploy-merge)
### `b7fa2a4` — Merge remote-tracking branch 'origin/deploy'
### `988334a` — fix: overwrite sidebar.tsx with deploy version after merge
### `ab211b9` — feat: re-apply NEAR wallet direct-signing flow on top of deploy
### `cee2584` — chore: gitignore auto-generated build artifacts
### `1f8058a` — fix: Metamask Type
### `64ea8f1`, `6553113` — fix: NEAR wallet direct-signing (1~2)
- NEAR 지갑 직접 서명 플로우 통합 (deploy 브랜치 머지 후 재적용).

### `4dc4d95`, `897cfa8` — feature / fix: refactoring Basics & Advanced topic
- 사이드바 AGENT SHOPPING → **AGENT SHOP** 리네임, 섹션 색 체리→회색 (HOT 배지 유지), Basics/Advanced **접기/펼치기** (YouTube 댓글 스타일, `localStorage` 저장, 기본값 접힘).
- 파일: `apps/web/components/cherry/sidebar.tsx`.

### `76e3c18` — feature: category enum
- 카테고리 enum 정리.

### `3ae9e21` — feature: file report
- 파일 리포트 기능.

### `7d331d4` — feature: knowledge curation
- `kaas.concept.created_by VARCHAR(100) DEFAULT '__SYSTEM__'` 컬럼 추가 (`kaas-concept-created-by.sql` 실행 완료).
- `findAllAdmin(createdBy?)` 필터 지원. `KnowledgeCurationPanel({ isAdmin })` prop 추가.
- 관리자: 전체 컨셉 / 일반 유저: 자기 것만. 생성 시 JWT userId 자동 저장.
- Prompt Templates 탭은 관리자만, Dashboard + Knowledge Curation은 항상 노출. Privacy Mode 토글은 탭 블록 밖으로.
- 파일: `kaas-admin-page.tsx`, `kaas-knowledge.service.ts`, `kaas-admin.controller.ts`, `kaas-dashboard-page.tsx`, `api.ts`.

### `1a47252`, `7909fb7`, `66a0774`, `fe35f4c`, `22c7448` — fix: compare (+ conceptTitle 1~4)
- Compare 응답에 `conceptTitle` 포함, gap 표시 정확도 보정.

### `24df89b` — fix: TEE purchase
- TEE 모드에서 구매 플로우 수정.

### `78cc864` — fix: knowledge diff
- 📚 Diff 비교 정확도 수정.

### `adf8c87` — fix: user id in market
- 마켓 화면 유저 ID 표시 보정.

### `b706832` — fix: windows parsing
- Windows 환경 파싱 버그 (`kaas-catalog-page.tsx`, `kaas-dashboard-page.tsx`).

### `4ab4ccc` — fix: deposit log
- deposit 로깅 개선. 목데이터 SQL 3종 추가: `cleanup-d701-d800.sql`, `stage-1-extra-100-apr16-18.sql`, `stage-4-extra-100-apr16-18.sql`.

### `73a777d` — feature: concept page publish
- **ConceptPagePublishPanel 최초 구현** (admin 패널).
- `kaas-admin.controller.ts` + `kaas-knowledge.service.ts` 확장.
- `apps/web/app/page.tsx` 레이아웃 조정. `apps/web/lib/api.ts` 에 publish API 함수 추가.
- 루트 `agent_read/post_concepts.py` (701줄) 삭제.

### `b97091d` — feature: concept page publish - 2
- ConceptPagePublishPanel 섹션/필드 확장 (`+106/-12`).

### `c9ba079` — feature: concept page publish - 3
- 최종 구조 확정. admin 패널 841줄 추가 / 461줄 삭제 대규모 리팩터링.
- 4단계 퍼블리시 워크플로우: 01 메타/slug → 02 Cherries (evidence) → 03 Content → 04 Progressive References (MECE 학습 경로) → Publish.

---

## [워킹 트리 — 미커밋]

- `.gitignore`: `apps/docs/arena/` 추가 (**공개 전** BMAD 산출물 임시 제외)
- `apps/web/app/page.tsx`: main content `max-w-[1440px] mx-auto` 래퍼 추가
- `apps/web/components/cherry/kaas-admin-page.tsx`: ConceptPagePublishPanel 일부 헬퍼 텍스트 정리 (slug 설명, Cherries 설명, Progressive References 설명, 바인딩 설명 삭제)
- `apps/docs/KaaS/onboarding-tour-plan.md` (신규, untracked): 말풍선 기반 온보딩 투어 기획서

---

## [다음 세션 참고]

- **상태 문서**: `api-backend-status-2026-04-20.md` (신규, 모듈 구조 + kaas 엔드포인트 전체)
- **세션 인수인계**: `handoff-2026-04-17.md` (04-17 기준 디테일 최다, KaaS 핵심 플로우 서술)
- **BMAD 기획 산출물**: `apps/docs/arena/` (gitignore, 공개 전 단계)
