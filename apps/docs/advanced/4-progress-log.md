# Advanced 6개 페이지 — 진행 로그

| 날짜 | 단계 | 내용 | 결과 |
|---|---|---|---|
| 2026-08-25 | 발견 | Advanced 6개 메뉴의 온톨로지 매핑 점검 | 6개 중 **1개만 정확** · 2개 틀림 · 3개 구조 어긋남 |
| 2026-08-25 | 자료조사 | 6개 항목을 외부 논문·서베이로 조사 → `research/` 7종 | 외부 출처 46건 · 항목별 표준 분류·원전·하위후보·경계·재료 정리 |
| 2026-08-25 | 기획 | 본 기획 4종 작성 | 신설 5 · 관계 22삽입/1해제 · 별칭 18 · 코드 1줄 |
| 2026-08-25 | **2차 검토** | 기획 자체를 검증 — 산술·인용·설계 | **오류 5건 발견·수정** |
| 2026-08-25 | **3차 검토** | B등급 인용 9건 원문 대조 + 재료 재측정 | **오류 3건 추가 발견** (E6·E7·E8) · A등급 17건 확보 |
| 2026-08-25 | 기획 보강 | **Phase 1-B 정본 동기화** 신설 (사용자 지적) | TTL·스냅샷 JSON·리서처 JSON 을 Phase 1 직후 전면 재생성 |
| 2026-08-25 | **승인** | D1~D10 전부 승인 | 착수 |
| 2026-08-25 | Phase 0-0 | 스크립트 이식 (브랜치 → deploy) | 5개 파일 · `.gitignore` 2건 · 리서처 JSON 재생성 → 검증 오류 1,216 → **0** |
| 2026-08-25 | Phase 0-1 | 기준선 스냅샷 고정 | `기준선-스냅샷-2026-08-25.md` (생성물) |
| 2026-08-25 | Phase 0-2 | `plan.cjs`(공유 정의) + `precheck.cjs` 작성 | precheck/apply/rollback 이 계획을 한 곳에서 읽는다 |
| 2026-08-25 | Phase 0-3 | precheck 실행 | ✅ 통과 · 경고 1건(역슬래시 8건 → Phase 1-B 이월) |
| 2026-08-25 | Phase 0-4 | **롤백을 apply 보다 먼저 작성** | `rollback-ontology.cjs` · 안전장치·재실행 안전 확인 |
| | | **Phase 1 대기 — apply-ontology.cjs 미작성** | 🔴 DB 쓰기 아직 없음 |

## 2차 검토에서 잡힌 오류 (2026-08-25)

| # | 오류 | 원인 | 조치 |
|---|---|---|---|
| E1 | 관계 총계를 **319** 로 계산 (실제 322 → 최종 331) | `QueryExpansion`·`QueryProcessing` 을 두 절에서 **중복 계수** | 재계산 |
| E2 | 별칭을 **12건 추가 / 19건 총계** 로 기재 (실제 18 / 25) | 표에 적은 항목과 합계가 불일치 | 재계산 |
| E3 | **`03 Child Concepts` 3개 이상** 기준을 6개 중 **4개가 미달** | children 이 "to 방향 관계"인데 신설 개념의 관계를 **바깥 방향**으로만 설계함. Custom Embeddings 는 **0개** | 신설 2→5개 · 연결 8건 추가 (§3-A) |
| E4 | The Prompt Report 대분류를 **6개**로 기재 (실제 5개) | 블로그 요약을 원문 확인 없이 옮김 | 원문 확인 후 정정 |
| E5 | **PAIR** 약어를 "Paraphrase & Inquire Response" 로 기재 | 검색 요약의 잘못된 풀이를 옮김 | *Prompt Automatic Iterative Refinement* 로 정정 |

| **E6** | 다중 턴 ASR `7.89~88.30%` vs `2.19~64.91%` | **원문에 없는 수치** (검색 요약이 만들어낸 값) | 실제값으로 교체 |
| **E7** | MTEB 영어 **7과제** (실제 8과제) | `Bitext mining` 누락 | 정정 |
| **E8** | 🔴 **"책 재료" 수치를 손수 고른 넉넉한 검색어로 쟀다** — 실제 도구는 노드명+별칭+하위개념만 쓴다. **Custom Embeddings 는 27건이 아니라 1건** | 측정 방식과 사용 방식이 달랐음 | 도구 기준으로 전면 재측정 |

**E1·E2 는 계산 착오, E3·E8 은 설계·측정 결함, E4~E7 은 2차 자료를 원문 확인 없이 옮긴 것.** 세 유형이 서로 다른 원인이므로 대응도 따로 세웠다 — 계산은 스크립트로, 설계는 시뮬레이션으로, 인용은 원문 열람으로 검증한다.

## 방향 결정 이력

| 날짜 | 내용 |
|---|---|
| 2026-08-25 | **지식팀 부재 확정** — 온톨로지를 우리가 직접 고친다(사용자 확정) |
| 2026-08-25 | **기존 한글 설명은 그대로 둔다**(사용자 확정). 새 개념 설명 언어는 D7 로 분리 |
| 2026-08-25 | `AdversarialEvaluation` **신설하지 않기로 제안** — 관련 개념이 이미 23개라 형제 중복이 생김. 제목으로 간극 흡수 |
| 2026-08-25 | **정본은 항상 전체다** — 델타만 내보내면 GraphDB 재구축 시 우리 작업이 사라진다(사용자 지적). 원본 305개를 포함한 임시 병합본을 만든다 |
| 2026-08-25 | 하위 깊이는 **학계 분류를 그대로 옮기지 않는다**(D5). PEFT Survey 는 60여 방법을 나열하나 우리 제품은 학술 색인이 아님 |

## 조사에서 확인된 사실 (기획의 근거)

| # | 사실 | 영향 |
|---|---|---|
| F1 | 책이 쓰는 말과 온톨로지 이름이 다르다 (`multiagent` vs `MultiAgentSystem`) | 별칭 등록만으로 후보 문단 **0 → 71건** |
| F2 | `handbook.book.source_type` 에 `WEB_URL` 이 이미 있다 | 외부 논문 적재에 **스키마 변경 불필요** |
| F3 | `concept.service.ts` 의 `menuLabel` 이 별칭 알파벳 첫 번째를 쓴다 | 별칭을 넣으면 **메뉴 이름이 바뀐다** → D8 신설 |
| F4 | 도서 `Reflexion`·`paper` 는 본문이 적재돼 있지 않다 (문단 0건) | 소장 도서는 사실상 **3권** |
| F5 | 🔴 Custom Embeddings 는 **책 재료가 도구 기준 1건**이고 `contrastive` 는 **0건** | **D6 이 이 페이지의 전제조건** (R9) |
| F6 | `handbook.concept` 에는 `origin` 칼럼이 없다 | 출처 구분은 `meta_json` 으로 |
| **E9** | 🔴 **Finetuning 하위를 6 → 5 로 예측** (실제 6) | 관계 이동의 **빼는 쪽만 세고 `CustomEmbedding --RELATED--> Finetuning` 을 더하는 것을 빠뜨림** | 적용 후 `verify.cjs` 가 잡음 · precheck 의 같은 버그도 수정 |
| **F7** | 🔴 **설명 8건에 역슬래시(LaTeX 수식)가 있다** — ALiBi·BPC·GELU·Planning·ReLU·RMSNorm·SwiGLU·TreeSearch | Turtle 리터럴에서 역슬래시는 이스케이프 문자다. TTL 작성 시 `\\` 필수. **precheck 가 잡았고, 그 전 수동 점검은 이걸 놓쳤다** |

## 기준선 (2026-08-25 · 고정)

```
handbook.concept                 305
handbook.concept_relation        310   (전부 origin='graphdb-import')
handbook.concept_alias             7
handbook.paragraph_chunk       3,054
handbook.paragraph_concept_link    7   (RAG 만)
content.concept_page               2   (RAG + 기존 a-mem)

Advanced 6개 페이지의 체리                0건
Advanced 6개 페이지의 References          0건
```

이후 "무엇을 추가했나"는 이 기준선과의 차이로 판정한다.

## 목표치

```
Advanced 6개 체리        0 → 30~42개  (D6 반려 시 5개 페이지 · 25~35개)
Advanced References      0 → 24건 (링크 열리는 것 12건 이상)
온톨로지                 305 → 310 개념 · 310 → 331 관계 · 7 → 25 별칭
정본 파일                llm_ontology_v3.ttl (전체 + delta) · ontology-snapshot.json
                         · concepts-to-fill.json (305 → 310 항목)
                         → GraphDB 재구축 시 이 파일들이 입력이 된다
```
