# Advanced 작업 — 기준선 스냅샷 (2026-08-25 · 고정)

> **생성물이다. 직접 수정하지 않는다.** 재생성: `node scripts/advanced/snapshot.cjs`
> 온톨로지를 바꾸기 **전** 상태다. 이후 "무엇이 바뀌었나"는 전부 이 값과의 차이로 판정한다.
> 계획 정의: `scripts/advanced/plan.cjs` · 사전 점검: `scripts/advanced/precheck.cjs`

---

## 1. 건수

```
handbook.concept                      305
handbook.concept_relation             310
handbook.concept_alias                  7
handbook.paragraph_chunk             3054
handbook.paragraph_concept_link         7
content.concept_page                    2
content.concept_page_contributor        0
handbook.book                           5
```

`plan.cjs` 의 `BASELINE` 과 일치해야 한다. 다르면 계획이 낡은 것이므로 즉시 중단한다.

## 2. 관계 분포

| 타입 | origin | 건수 |
|---|---|---:|
| SUBTOPIC | `graphdb-import` | 304 |
| RELATED | `graphdb-import` | 5 |
| PREREQUISITE | `graphdb-import` | 1 |

> `EXTENDS` · `CONTRADICTS` 는 0건이다. 원본 온톨로지에 해당 술어가 없었다.

## 3. 별칭 7건

```
Chunking                 → Chunking Strategies   (VARIANT)
Embedding                → Embeddings   (VARIANT)
Finetuning               → Fine-tuning   (VARIANT)
HybridRetrieval          → Hybrid Search   (VARIANT)
InferenceOptimization    → InferenceOptimization   (VARIANT)
RAG                      → Retrieval-Augmented Generation   (VARIANT)
VectorDatabase           → Vector Databases   (VARIANT)
```

## 4. Advanced 6개 페이지의 `03 Child Concepts` — 변경 전

| 노드 | 지금 | 적용 후(기대) | 하위 개념 |
|---|---:|---:|---|
| `AdvancedPrompting` | **11** | 11 | ChainOfThought · PAL · PlanAndSolve · ProgramOfThoughts · PromptChaining · ReAct · Reflexion · SelfAsk · SelfConsistency · SelfRefine · TreeOfThoughts |
| `MultiHopRAG` | **0** | 5 | (없음) |
| `ParameterEfficientFinetuning` | **0** | 3 | (없음) |
| `MultiAgentSystem` | **0** | 4 | (없음) |
| `CustomEmbedding` | **0** | 3 | (없음) |
| `RedTeaming` | **0** | 3 | (없음) |

> 6개 중 5개가 **0개**다. 화면에서 03 구획이 비어 있다는 뜻이다.

## 5. 소장 도서

| 도서 | 문단 |
|---|---:|
| Building Applications with AI Agents | 1114 |
| AI Engineering | 877 |
| LLM Engineers Handbook | 670 |
| paper | 0 |
| Reflexion | 0 |

> `paper` · `Reflexion` 은 행만 있고 본문이 적재돼 있지 않다. 사실상 3권이다.

## 6. 이 스냅샷이 담지 않는 것

```
체리 내용 · Overview 본문 · References — 개념 페이지 콘텐츠는 아직 RAG 한 장뿐이다
```
